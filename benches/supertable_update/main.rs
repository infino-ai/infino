//! Supertable update/delete throughput diagnostic bench.
//!
//! Pending custom-harness migration. This target intentionally does not
//! run the old benchmark implementation.

fn main() {
    eprintln!(
        "supertable-update is pending custom-harness migration; see bench-harness-migration-plan.md"
    );
}
